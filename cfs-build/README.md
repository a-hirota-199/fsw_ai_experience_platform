# cfs-build — 軽量検証イメージ

生成された cFS アプリ骨格を**ビルド**して通る/通らないを返す軽量ゲート。別リポジトリの計画にある「検証ハーネス」の**ビルド段だけ**を借用する（厳密な4ゲートはMVP対象外）。

- Dockerイメージで cFS Bundle のビルドに必要な環境（cmake / build essentials 等）を固める。
- 結果（終了コード＋ログ抜粋）を backend に返し、失敗時は「AIが直す対話」へ接続する。
- 体験を止めないため、ビルド段はSkip可能なフラグを持たせる。

Sprint 3 で着手。
