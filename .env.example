# LLM provider: mock | anthropic   (Sprint 0 は mock)
LLM_PROVIDER=mock

# frontend → backend の接続先
BACKEND_URL=http://localhost:8000

# Anthropic API キー（Sprint 1+ で anthropic provider を使う時のみ）
ANTHROPIC_API_KEY=
